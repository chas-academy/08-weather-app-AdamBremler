import styled from 'styled-components';
import { Form } from 'reactstrap';

const NavFormContainer = styled(Form).attrs({
    className: 'col-10 col-sm-10 col-md-6 p-0 align-items-start'
})``;

export default NavFormContainer;
